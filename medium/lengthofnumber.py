
#! Length of Number

# Create a function that takes a number num and returns its length

def number_length(num):
    num_str = str(num)
    for i in num_str:
        return sum(i)

number_length(10)
number_length(5000)
number_length(0)

# Attempt 2