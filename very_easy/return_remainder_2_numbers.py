# There is a single operator in Python capable of prividing the remainder of a division operation.
# Two numbers are passed as parameters.
# The first parameter divided by the second parameter will have a remainder, possibly zero.
# Return that value.

def remainder(x,y):
    remainder_form = x % y
    print(remainder_form)
    return remainder_form

remainder(3,2)

