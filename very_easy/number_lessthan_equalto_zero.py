# Create a function that takes a number as its only argumnet and returns True if it's less than or equal to zero, otherwise return False

def less_than_or_equal_to_zero(number):
    if number <= 0:
        return True
    else:
        return False